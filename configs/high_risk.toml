# Director-Class AI — high-risk profile: stricter thresholds, audit + approval
name = "high_risk"
default_dry_run = true
content_threshold = 0.35
action_block_threshold = 0.2
uncertainty_margin = 0.25
require_audit = true
require_approval = true
