# Director-Class AI — pilot profile: durable audit + human approval required
name = "pilot"
default_dry_run = true
content_threshold = 0.5
action_block_threshold = 0.3
uncertainty_margin = 0.2
require_audit = true
require_approval = true
capability_profile = "local_operator_actions"
